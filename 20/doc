package main // import "."

Разработать программу, которая переворачивает слова в строке. Пример: «snow
dog sun — sun dog snow»

FUNCTIONS

func flipStringByWords(src string) (flip string)
    flipStringByWords переворачивает предложение. Читает строку, выбирая целые
    слова, полученные слова пишет задом наперёд в результирующую строку

func main()
