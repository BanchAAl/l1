package main // import "."

Разработать программу, которая переворачивает подаваемую на вход строку
(например: «главрыба — абырвалг»). Символы могут быть unicode

FUNCTIONS

func flipString(src string) (flip string)
    flipString переворачивает строку. идёт по строке и пишет в результирующую с
    конца

func main()
