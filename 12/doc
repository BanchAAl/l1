package main // import "."

Имеется последовательность строк - (cat, cat, dog, cat, tree) создать для
нее собственное множество.

FUNCTIONS

func createSubset(set []string) (err error)
    createSubset создаём собственное подмножество. Оно должно принадлежать
    исходному множеству, но не должно быть равно ему и не должно быть пустым.
    Выбираем уникальные значения, если это не удовлетворит условию, просто
    выкинем последний элемент

func main()
